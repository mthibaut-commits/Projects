---
type: conocimiento
title: "Verificación: qué cubre cada paso y cada caso de la suite"
description: "El paso 0 (el formato) y los siete pasos que se corren tras editar el fuente y, caso por caso, qué defecto fija cada uno de los 140 de la suite; más las trampas de run_tests y de las capturas"
tags: [conocimiento, verificacion, tests]
timestamp: 2026-09-17T22:29:11Z
---

# Verificación (correr SIEMPRE tras editar)

> Verbatim del `CLAUDE.md` anterior al 17-09-2026. Los comandos, en el orden en que se corren, están en `CLAUDE.md` (sección *Verificación*); este documento es el **mapa de la suite**: qué invariante fija cada caso y qué defecto lo motivó. Es la semilla de la columna *dónde se verifica* de [`invariantes.md`](./invariantes.md).

0. `npx prettier@3.6.2 --check pipeline_comercial.jsx` — **el formato** (ADR-0006, 18-09-2026). No verifica una conducta: protege a los que sí. Veintiún gates `regla_<slug>.test.mjs` y los dos auditores leen el fuente como TEXTO y están re-anclados contra el `.jsx` formateado; un commit que deshaga el formato los tumba de a uno, en sesiones distintas, sin que nadie relacione la caída con el formato. Se arregla con `npx prettier --write pipeline_comercial.jsx`, nunca aflojando el gate que cayó. Los patrones de esos gates trabajan sobre el texto que devuelve `canonico()` (`tests/contract/_comun.mjs`): colapsa los espacios, saca la coma final antes de un cierre y aprieta los corchetes, así un patrón escrito contra el fuente de una línea sigue calzando y deja de depender de dónde caen los saltos.
1. `npx tsc --jsx preserve --allowJs --noEmit --skipLibCheck pipeline_comercial.jsx` — sin errores TS1.
2. Duplicados: `grep -oE '^(export default )?(async )?(function|const|let|var|class) [A-Za-z_$][A-Za-z0-9_$]*' pipeline_comercial.jsx | awk '{print $NF}' | sort | uniq -d` debe salir vacío.
3. `node build_app.mjs` — construye y valida los hashes del vendor.
4. `node --test "tests/contract/*.test.mjs"` — **los gates de contrato** (~10 s, sin navegador): la forma del vault, del índice de reglas, del fuente y de la suite, las líneas base de los auditores, los hooks, y desde el 17-09-2026 un archivo `regla_<slug>.test.mjs` por regla que se fija sobre el TEXTO del fuente (73 archivos en total). Qué fija cada uno: la tabla *Gates de contrato* de [`invariantes.md`](./invariantes.md).
5. `tests_asignacion_lineas.js` — **179 casos**: los 15 de la asignación misma, 5 del **diff entre versiones** (§4.3, donde el 20 fija que la versión anterior NO altera el resultado) 3 del **recorte** de una operación aceptada (§9 del spec de verificación, donde el 23 fija que recortar no re-asigna) 3 de la **reapertura** (el 24 fija que reabrir revoca la firma) 4 de la **mesa de verificación** (que se listen todas las causas y que las filas agrupen por deudor) 2 de la **compuerta del tab de Verificación** (que cerrar sin publicar no basta) 2 de los **roles del tenant** (que sólo el Ejecutivo de verificación firma una verificación) 3 de la **atribución por rol** (que un cargo vacante lo cubre su jefatura y que la escalada no cruza áreas) 4 del **ruteo de excepciones** (que una excepción de Operaciones la aprueba Operaciones, que a mayor gravedad va mayor jerarquía, que ningún tramo queda sin aprobador y que una regla sin área no la aprueba nadie) 1 del **catálogo de áreas**, 1 de **«Sin aprobador definido»** y 2 del **desacople del motor** (que decide con un padrón inyectado, y que el visado y el commit de verificación entran por parámetro) 3 de la **cartera del par cliente-deudor** (que el catálogo implemente las 79 reglas de la política, y que C47–C50 se evalúen y se visen por deudor y no por cliente) 1 de que **el Comité no es un nivel aparte** (C05 se rutea como todas y ninguna regla declara un nivel fuera de N1..N5) y 2 del **piso por monto** (que el monto suba el nivel exigido sin bajarlo nunca, y que el modelo paralelo de causas de desvío se haya retirado entero). El caso 40 prueba cada tramo contra **cada tramo de monto**, no sólo con su nivel base: es lo que detecta un piso que pide más de lo que su área alcanza. 4 de la **inyección de estado** (12-09): que el motor de otorgamiento decida con las variables inyectadas, que el veto de la verificación y el visado que libera el giro entren por parámetro —y que el cache no envenene—, y que los nombres de los aprobadores salgan del padrón y no de `USERS`. Y 4 de los **defectos del predictor de verificación** corregidos el 11-09: que la verificación pendiente se cuente contra las llamadas registradas y no contra un sorteo (52), que el segmento del par no dependa de qué pantalla preguntó primero (53), que la regla 6 sin plazo incumpla (54) y que el veredicto congelado mantenga su fila en la mesa (55). Y 4 de **vacaciones y reemplazos** (12-09): que la atribución se preste sólo durante el período y que el ausente no la pierda (60), que la bitácora estampe el campo y la glosa de reemplazante (61), que el storage se sanee y que cubrir a alguien nunca BAJE de nivel (62), que lo que el motor deja aprobar la **pantalla** lo deje ver y firmar (63) —el primer intento tenía el motor correcto y el badge descartaba al reemplazante antes de preguntar—, y que el ausente siga aprobando salvo que el reemplazo se lo revoque (64). Y 4 de **pricing y simulación** (12-09): que el **resumen cuadre con el detalle** por factura —la diferencia de precio del catálogo, con tasa y plazo equivalentes, da la suma documento a documento— (65), que las fórmulas se interpreten y no se ejecuten como JavaScript (66), que la validación ataje la referencia adelantada y las otras cinco formas de romper el giro (67), y que lo que el tenant configura cambie el giro (68). El 69 hace lo mismo con la TASA: el spread de lista y el descuento por SOW salen del tenant y el piso del deudor sigue truncando. Y 5 del **prorrateo por factura** (12-09): que reproduzca la planilla del negocio —descuento racional y plazo ponderado por dif. de precio, donde ponderar por monto daría 46,5 días en vez de 52,79— (70), la **regla de oro** contra 60 carteras generadas, con la cota teórica del ajuste y sin ningún documento negativo (71), que el ajuste caiga en la factura más grande (72), que la tasa única use el plazo de cada documento (73) y los bordes —un solo documento, plazo 0, tasa 0, cartera vacía— (74). El 74 encontró que `prorratearConcepto` reventaba con una oferta sin facturas, que es un estado normal de la pantalla. El **75** fija la precisión: calcular con la tasa redondeada a los 2 decimales que se muestran mueve la diferencia de precio 15.764 pesos. Y 2 de la **regla 0 de verificación** (12-09): que la primera operación del cliente verifique todas las facturas y cortocircuite las demás reglas, incluida la del protocolo propio (76), y que el estado del cliente entre por parámetro (77). Y 5 de la **asignación de giros** (12-09): que Express exija las dos condiciones y que el tipo se herede del deudor (78), que la primera operación y una excepción del cliente manden todo a Normal (79), la **regla de oro** contra 50 carteras —ninguna factura sin tipo y la suma siempre igual al monto a girar— (80), que el motor decida con los veredictos **inyectados** y que el catálogo de tipos sea extensible a un tercero sin tocarlo (81), y que la asignación congelada al aceptar mande sobre el recálculo del día (82). El **83** cubre el camino de las LISTAS: que la tarjeta reparta el giro simulado y cuadre con él, que sin simulación no muestre nada, que memoice, y el desajuste de ids posicionales que dejaba los chips en blanco sin error. El **85** fija **O05 · Contrato firmado por cliente de la operación**: que el criterio exista siempre, que lo cierre la **huella** del paquete autorizado, que agregar una factura después lo vuelva a abrir, que la sustitución que conserva conteos y total tampoco pase, y que el cargo de Operaciones N3 exista **y esté ocupado** —el primer intento creó el cargo y la excepción seguía cayendo en el N5 porque nadie lo tenía—. El **86** cubre el gate de inyección al core (GIR-02): que distinga «no hay evidencia» de «la evidencia no describe esta operación», y que la huella sea estable al orden de las facturas. El **88** fija que **firmar no es girar**: las tres compuertas mandan sobre el atajo del otorgamiento automático, y ninguna de las 16 combinaciones con algo pendiente termina en giro. El **87**, la **rotación de personas**: que el alcance de una jefatura se derive del equipo y que un usuario desconocido falle CERRADO, que un código que el padrón ya no conoce no se relabele como «Agente IA», y que una tarea de aprobación siga al par (área, nivel) y no a la foto de nombres. El **84** fija que el giro por factura suma el **«Monto a Girar» de la simulación** y no el anticipo neto de intereses: el detalle prorrateaba con la lista de conceptos VACÍA —le servía para la tasa y el plazo equivalentes— y el chip de la cabecera quedaba ~2 MM por encima de la cifra de la fila de abajo, la regla de oro rota en la misma pantalla que la enuncia. Se pegan en la consola del navegador con el HTML abierto, o se corren solos con **`node run_tests.mjs`** (`PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers`; Chromium ya viene en el contenedor y el script resuelve playwright solo). Dos trampas anotadas en ese archivo: **`nex-pipeline` renderiza en light DOM** —no tiene `shadowRoot`, así que esperar por él se cuelga aunque la app esté montada, la señal es el texto del login— y las opciones de `waitForFunction` van **terceras**, si no rige el timeout por defecto de 30 s y no alcanza (31 MB y Babel transpila ~21.000 líneas en el navegador, ~1 min). El **89** fija que entre DTESync y el motor no se pierde un peso. El **90**, que las perillas del mantenedor MANDAN: inyecta un tenant que contradice al del navegador —que es la única forma de distinguir «lee la configuración» de «coincide con el default»— y comprueba el piso de tasa, la ventana del libro de ventas y la glosa del comité — y, desde la regla 44, que `otrosDeudoresPct` **ya no dimensione** el comodín LF4 desde la app: la perilla es declarativa y mover el mantenedor no puede re-tallar un cupo que otro sistema aprobó. Se exige además que la LF4 EXISTA, porque «no se movió» se cumpliría sola con dos ceros. El **91** fija el activo **A24 · Cartera comercial**: que los cuatro catálogos de la estructura sean un índice sobre el archivo y no contradigan ninguna fila, que el alcance de una jefatura salga de la arista `COD_JEFE` (JG → [CR, RF]) y que un jefe que el archivo no menciona no vea nada, que la asignación de cartera salga del A24 y no del campo pasajero del A5, que un RUT ausente sea un prospecto sin dueño y no un cliente huérfano, y que ninguna asignación apunte a un código que el archivo no declara. El **92** fija que el libro de ventas no se re-sortee al incorporar una factura: ningún folio nuevo aparece, el incorporado sale del pool y no se vuelve a ofrecer, y el deudor y el monto de los que quedan no se mueven. Y el **93** fija que las **fechas de la factura son un dato**: el resolver no mira el reloj (se adelanta `Date.now` 40 días y devuelve lo mismo), lee `FchEmis`/`FchVenc` del activo en vez de recalcularlas, la misma factura da las mismas fechas como candidata y como incluida en la oferta —que es el defecto que reportó el usuario—, y el plazo sale de la resta de las dos fechas y no del `45` cableado. Y el **94** fija que **las facturas salen del activo**: que ningún documento del libro falte en DTESync y que su deudor, RUT, monto y fechas calcen fila a fila, que un cliente que el archivo no declara **no tenga libro**, que los estados del documento (nota de crédito, reclamada) salgan de `EstadoDTE` y no de un hash —ninguna factura limpia bloqueada, donde el hash bloqueaba ~29%—, que «en otra operación» entre por parámetro por ser estado del pipeline y no del SII, y que el asistente de alta manual lea el mismo libro. Y el **95** fija que **una factura cedida es una factura que existe**: que toda cesión del A2 apunte a un documento real de su cedente con los campos copiados del A1, que se cumplan los dos invariantes del activo —ninguna cesión anterior a la emisión y ninguna por más que el documento, con la cota «o menor» **ejercitada** por 160 cesiones parciales— y que no haya folios cedidos dos veces ni cesiones sobre documentos no cedibles, que el bloqueo nombre al factoring y distinga la cesión propia de la ajena, que un documento del mismo cedente que nadie cedió siga disponible —el bloqueo no se contagia al cliente entero—, y que la pérdida por cesión y el competidor salgan de las facturas de ESA oferta y no de un sorteo. Y el **96** fija **O06**: que la regla exista y sea de Operaciones, que sus dos tramos vayan a niveles distintos (N5 la cesión excedida, N3 la parcial), que la variable se MIDA sobre las facturas de la oferta contra el A2 —una oferta con un documento cedido en parte levanta la excepción y la misma sin él no—, que Operaciones alcance los dos niveles para que ningún tramo quede «Sin aprobador definido», y que la política no la traiga: sus dos reglas de cesión (C37, C39) son de concentración y del área comercial. Y el **97** fija la partición de «Deudores disponibles»: que el criterio sea por FACTURA y no por el total del deudor (con 40 de línea y facturas por 137, caben dos), los dos extremos, que un deudor «Otro» con LF4 entre igual —el criterio no filtra por Prime—, que las compuertas no sean siquiera parámetros de la función, que las bloqueadas no cuenten, y que la partición sea exhaustiva y disjunta sobre el libro real. Y el **98** fija la vista «Por factura»: orden por folio descendente estricto, que traiga **lo mismo** que la agrupada —ni una factura de más ni de menos, que es lo único que separa una vista de un filtro—, que se componga con las pestañas trayendo sólo los deudores que se le pasan, los bordes (sin deudores, deudor ausente del índice) y que **no mute** el índice del que lee: `sort` ordena en sitio y reordenar la lista plana no puede reordenar las facturas dentro de cada acordeón. Y el **99** fija el **mix de financiamiento**: que las cuatro porciones sumen 100, que el **padrón de cesionarios** declare a los 15 que el A2 trae y que Eurocapital NO sea banco, que la porción propia quede **anclada** al `SOWActualPct` del A5 y el resto se reparta con la proporción **medida en A2**, que el **detalle por cesionario** cuadre con su chip, que un deudor devuelva `null` y no cuatro ceros, y que una porción en 0% no se dibuje salvo la nuestra. Y el **100** fija que «con línea» en el tubo es un **lookup** y una **COTA**: que el criterio sea por factura y no «¿queda algo?» —que no descartaba a nadie, 0 de 7.100—, que el pozo comodín sea uno solo y la LF1 sólo para Prime, que un deudor desconocido falle cerrado, que «sin línea» no distinga Prime, y la invariante que lo justifica: **todo deudor al que el motor asigna está entre los que el atajo declara con línea**, 0 violaciones en 60 carteras, sobreestimando 2,4×. Y el **101** fija que **A5 se deriva de A2**: que la participación de cada cliente sea la que miden sus cesiones (233/233 dentro de 0,05 pto, donde antes el peor desvío era 92,6), que la serie semanal cuadre **monto a monto** contra el registro, que el eje de semanas sea común, que ninguna semana sea anterior a la primera cesión —el defecto de fondo: A5 declaraba 9.104 cesiones y A2 tenía 1.300—, que el `SOWTargetPct` siga siendo una META y no se mida, y que los invariantes del registro sobrevivan a haberlo hecho seis veces más grande. Y el **102** fija el **mínimo de línea aprobada**: que ninguna LF1/LF2/LF4 quede bajo el piso entre los clientes que pueden pagarlo, que la **LF3 exenta** lo esté de verdad (17 de 399 por debajo), que el **tope del cliente mande sobre el piso** —sin esa cota 3 clientes recibían más de lo aprobado—, que el activo tenga la forma que eso implica —hay clientes con deudores en la COLA, sin línea propia, financiados por el comodín— y que mover `lineaMinima` **no re-dimensione** nada, porque la estructura es un insumo (regla 44). El reparto con piso en sí lo prueba `regla_lineas_activo.test.mjs`, donde vive la función desde que dimensionar salió del pipeline. Y el **103** fija que el **factoring target es configuración del TENANT** y no un atributo del cesionario: que el default sea BCI + Santander y el rótulo del chip se arme con ellos, que mover la perilla mueva la porción de un balde al otro **sin mover la medición** —lo nuestro y el total ajeno quedan iguales—, que el cache del mix se invalide solo, que un target NO bancario siga partiendo bien (la partición se mantiene exhaustiva y disjunta), la higiene del storage —fuera del padrón, el nuestro o repetido no entran— y que sin nadie en el target la porción no se dibuje. Se prueba **inyectando una configuración que contradice al default**, como el 90. Y el **104** fija que la **CAT es del paquete que se COMPRA**: que un conjunto vacío no sea «CAT-1» —devolvía la MEJOR categoría con cero datos—, que un `facturasOp` vacío sea una respuesta y no ausencia de dato, que la CAT de la oferta **no dependa de simular** (la misma oferta con y sin `simulado` da lo mismo), que los cortes 1/2/3/5 sigan intactos y que un CAT desconocido no se pinte del color del mejor. Y el **105** fija los **chips de la columna SOW**: que nombren a los cesionarios y no a las porciones, las dos ramas (nosotros arriba → 3 nombrados + «Otros»; nosotros fuera → 2 nombrados + «Otros» + nosotros al final, con % o con cero), que «Otros» agrupe **sólo lo ajeno** —contarnos ahí nos contaría dos veces—, los bordes (sin mix, sin detalle, un solo cesionario) y, sobre el archivo real, que en las **258 empresas** los chips sumen 100, sean a lo más 4, nos incluyan exactamente una vez y «Otros» cuadre con su tooltip. Y el **106** fija que **cerrar la oferta pide la línea solo**: que sin nada que pedir no se invente una solicitud, que salga **UNA** con N líneas de detalle —una por deudor, con lo que faltó y en **puntual**—, que lo pedido se **sume a la vigente** (650 + 90 = M$740, porque `constituirLinea` escribe ese campo como la aprobada y mandar sólo lo pedido dejaría al cliente con MENOS línea el día que se la aprueban) y que el wizard la encuentre pre-cargada. Y el **107** fija que la solicitud **viaja en pesos** y el borde que la constituye no la redondea: un cupo **no redondo** (287.431.509) llega intacto —un monto redondo sobrevive a una división por un millón y no distinguiría nada—, `propFactoring` manda sobre `totalPropuesto`, renovar conserva el uso y recalcula al peso (`toFixed(1)` dejaba décimas de peso en el disponible contra el que el motor decide si una factura cabe) y una solicitud en cero no constituye ninguna línea. Y el **108** fija que una oferta con **«Monto a Girar» no positivo se simula pero no se cursa**: el corte está en $1 y no en cero —no se transfieren $0—, redondea al peso antes de juzgar, **sin simular no se pronuncia** (regla 14: no se bloquea por una cifra que nadie calculó) y el resultado trae siempre el **motivo**, porque un CTA apagado sin explicación deja al ejecutivo sin dónde enterarse. Y el **109** fija el **detalle de la solicitud al comité**: que las líneas del par se SUMEN (LF2+LF3 son dos cupos del mismo par) y que una **descartada** no cuente —el motor la fusionó en su hermana y sumarla contaría dos veces—, que **«sin línea propia» no sea una línea en cero** ni se cuele ahí el comodín del cliente, que el disponible no se vaya bajo cero con la línea copada, que la proyección suba los **dos** lados (aprobar sin proyectar el uso mostraría una línea que se amplía y nunca se ocupa) y que la función no mute el índice del que lee. Y el **110** fija que la **etapa de una operación se rotula en un solo sitio**: los dos colapsos (giro → Aceptada, oferta publicada), que lo que no colapsa pase tal cual, que el rótulo calce con el que dibuja el chip —si las dos rutas dieran distinto, el chip y el texto de al lado se contradirían—, que siga saliendo del catálogo del TENANT y que una operación nula no reviente la pantalla que la pide. Y el **111** fija que el **tubo y Operaciones nombran el mismo estado igual**: que `etapaDeDeal` y `estadoOperacion` coincidan en los seis estados, que los cuatro de la máquina posterior a la firma sean **distintos entre sí** —si dos colapsaran, el ejecutivo no podría saber qué falta—, que antes de firmar mande la etapa del TENANT y que `fueraDelTubo` parta bien las seis. Y el **112** fija el **orden de la lista de oportunidades** —avance primero, plata después, con la pérdida al fondo y la oferta publicada por delante— y que la clave sea pura y estable. Y el **113** fija que **lo que el detalle escribe en un repositorio lo vea la siguiente pestaña**: que `set`/`patch`/`push` **y `del`** persistan, que el scope por tenant viaje con el dato, que un storage corrupto parta de cero en vez de reventar, y que el tubo siga siendo el dueño de la vida útil. Y el **114** fija que **solicitar no cierra la puerta**: que sin solicitud previa no se escriba nada —una ampliación sin destinatario no la mira nadie—, que se **apile sin pisar** la justificación original, que cada ampliación lleve su propio actor y su propia hora, que sean append-only y en orden, y que una vacía no escriba. Y el **115** fija que el **predictor de verificación compara PESOS contra PESOS**: que una operación normal pase V03/V04/V09 —que antes no los pasaba nadie—, que el techo por monto **siga discriminando** (una operación sobre el umbral falla), que el borde esté en M$300 exacto y no un millón más acá, que V10 siga midiendo sobre M$1.000, que el valor se muestre con `fmtMM`, y que las tres variables del par se lean del A10 como MILES×1000 **contra el activo real** (200/200 filas) y no contra un orden de magnitud, que una conversión equivocada por 10 también pasaría. Y el **116** fija que **cerrar la oferta se deshace con «Editar» y que publicar y firmar NO se deshacen con ella** (regla 32): el predicado del cierre mira las dos banderas y la marca de edición, `ofertaPublicada` no se cae al editar —si se cayera, el tab de Verificación desaparecería con las llamadas ya registradas—, a quién aplica editar (a la cerrada y a la firmada sí; a la girada, a la que ya tomó Tesorería y a la perdida no, **y las tres con motivo distinto**), que la firma se revoca sólo si la había y que volver a cerrar **no la devuelve**, y que la solicitud al comité no se duplica al re-cerrar con el mismo detalle. Desde el 17-09-2026 los **117–140** cierran la tabla de invariantes (un caso por regla que iba sólo por revisión; los escribió un agente por fila y otro los refutó — log de esa sesión): **117** la Nota Deudor decide y no el score, la compra lee `notaMinCompra` del tenant y el corte 4,2 es estricto (regla 2) **118–119** la pérdida es terminal: causa específica en los seis escritores, etapa de origen, desenlace cerrado, cascada de tareas, y la segunda mitad —el rechazo sin motivo, el lector sin el genérico y la bitácora con su actor— que destapó los defectos corregidos en este commit (5) **120** el mínimo del deudor es su piso más el costo de fondo del tenant y la escalera de atribución mide contra esa referencia (8: la cláusula «el cliente pide bajo el mínimo» no existe en el código, ver el tablero) **121** contactabilidad: no entregado ⇒ un intento, entregado sin respuesta ⇒ hasta tres (10) **122** ejecutivo por cedente, «Otro» puro no abre y las candidatas salen de proveedores × cesionarios (11) **123** el motor evalúa sin persistir y una aceptada se recorta, no se re-asigna (12) **124** un solo indicador de línea, «Disponible» = aprobada − utilizada y «queda» sólo con la oferta simulada (13-terdecies) **125** una reevaluación es UNA versión que congela línea y verificación sobre las mismas facturas (14) **126** NEX inyecta y consulta: sólo «Aprobada» constituye la línea (15) **127** la solicitud cruza de pestaña con el registro ya armado, una vez por `idProceso` (15-bis-bis) **128** `InputPesos` muestra el peso con miles y emite el número (15-quater-bis) **129** el Command-K encuentra «Reportes» y «Gestión diaria» por el rótulo que muestran (27-bis) **130** `construirDirectorio` arma 5 operaciones con facturas reales, determinista y sin persistir (31) **131** teléfonos ofuscados, marcas absolutas y auditoría encadenada (17) **132–140** los nueve invariantes del contrato que iban sin gate: tenant de la sesión (TEN-01), tope por usuario y familia (RAT-01), duplicado contado (IDM-01), la operación cabe en la línea (LIN-01), sólo aprueba quien tiene atribución (OTG-01), no gira sin cesión (GIR-01), descuento dentro de la atribución (ATR-01), OTP con SHA-256 y sal (CRY-01) y la prioridad de curse de la jefatura (PRI-01). Y el **141** fija que una regla con un criterio **sin área** no se ejecuta ni se verifica, sale nombrada en el veredicto y no bloquea (regla 35): la compuerta es pura, exime a las de clasificación y a las sin tramos, falla cerrado con `null`, y el caso PLANTA la regla en el catálogo y lo restaura. Y el **142** fija que la **Bandeja Inbound es una ventana con tope** (regla 40): lo nuevo entra adelante, la perilla alcanza para un lote entero —con el tope viejo de 60 un lote de 250 botaba 190 en silencio—, sale primero lo que no es de nadie y lo que sale se CUENTA. Y el **143** amplía la regla 35 al otro extremo del ruteo: una regla **excepcionable a la que nadie puede firmarle la excepción** tampoco se ejecuta. Mide las tres causas por separado —sin área, área que el tenant no tiene, nadie en ese nivel ni en uno superior— porque se arreglan en tres mantenedores distintos; prueba que lo que manda es el **padrón inyectado** (la misma regla, el mismo tenant: con el cargo se ejecuta y sin él no), que se miran **todos** los tramos de excepción y no el primero, que el **knock out sigue corriendo** con el padrón vacío, que sin padrón falla **cerrado** —quien olvide inyectarlo ve el criterio marcado, no aprobado en silencio— y que la regla plantada llega hasta el veredicto de una operación por el camino real. Y deja medido que **hoy el catálogo no tiene ninguna**: las 69 con excepción tienen a quién pedírsela en este tenant, así que el día que alguien borre un área o la deje sin gente, este caso se rompe. Y el **150** cierra el bucle del comité (regla 44): el deudor que va al comité **por no tener LF2/LF3** vuelve con una puntual y la asignación siguiente la usa a ella y no al comodín. Se prueba en las **dos direcciones** —antes ninguna línea de par lo financia, después todas sus facturas salen de la LF3 nueva—, más que el techo del cliente suba con ella y que el mismo `idProceso` no constituya dos veces, porque la API 3 se consulta en cada refresco. 6. `PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers node tests/e2e/correr.mjs` — **los casos e2e** (`tests/e2e/*.e2e.mjs`, ~8 min): la app con la sesión iniciada (el OTP se lee de la pantalla), el Modo Directorio para tener operaciones sin stream y el detalle abierto en su pestaña. El runner reinicia el estado al empezar cada ARCHIVO (Directorio apagado, filtro «Con línea», sin modal); dentro del archivo los casos se encadenan como los ordenó su autor. Un caso por regla de pantalla, citado en `invariantes.md` como `e2e-<regla>`: `e2e-12-bis-a…e` (la simulación saca de Prospección y el dual), `e2e-13-octies-bis-a/b` (el vacío se ve), `e2e-13-terdecies` (la línea en la cabecera), `e2e-13-quaterdecies` (vaciar la oferta), `e2e-13-sexdecies-a…d` (armar a mano y retirar la última), `e2e-14-a/b/c` (reevaluación explícita; el `c` fija el defecto del pie), `e2e-15` (la bandeja «Solicitudes»), `e2e-15-bis-bis-a/b` (la solicitud cruza de pestaña), `e2e-15-quinquies` (el documento con el registro inyectado), `e2e-15-quater-bis` (el paso de deudores del wizard), `e2e-27-bis` (los rótulos del menú), `e2e-29-a/b` (pesos en el documento), `e2e-30` (el menú «Acciones»), `e2e-31` (Modo Directorio), `e2e-58` (la oferta publicada como estado en el tubo), `e2e-59-a`/`-b` (el tab de Verificación informativo al simular, y la otra dirección: pre-evaluar abre la llamada), `e2e-76` (Reportes › Cliente y SOW con la cartera medida, cruzada contra el Dashboard, y el alcance que mueve las cifras) y `e2e-77` (el Dashboard y Performance comercial dicen lo mismo del mes, con la participación frente al target a la vista) y `e2e-CRY-01-limite`/`-bajo-limite` (el límite de intentos del OTP). `00_sesion` es el humo del harness.7. Si el cambio toca la UI, regenerar las capturas: `PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers node capturar_pantallas.mjs`. Es lo que consume el export a Figma; si no se regeneran, el export vuelve a mostrar una versión que ya no existe. Y el **157** fija que **la mesa de verificación trabaja por FACTURA** (regla 53): cada documento trae su estado y su respaldo, el estado del deudor es el resumen de los suyos, y la factura RETIRADA sigue en la mesa —sale de `facturasOp`, así que listándolas sólo desde ahí la evidencia de «ésta no la confirmó» desaparecía de la pantalla, y con todas retiradas el deudor entero se esfumaba—. Y el **158** fija que **mientras se simula los pendientes son un pronóstico y al publicar se exigen** (regla 54): el color de los badges y de los chips sale de una sola compuerta, `exigeAcciones`, y se prueba en las dos direcciones y en el borde —cerrada sin comunicar no basta: publicar son dos hechos—. Los **172–175** fijan la revisión de Reportes del 23-09-2026: el **172** que la cartera de Cliente y SOW se MIDE (regla 76: «Solo competencia» es el mismo conjunto que «sólo otros» del churn, la desviación es una partición, y el donut, las series y los competidores salen del A5/A2 del alcance, sin el tenant); el **173** que las cifras de operación del Dashboard y de Performance comercial se suman semana a semana del A1 y del A2 con una sola función (regla 77), contra un recuento directo de los dos activos; el **174** que la pérdida por cesión es un hecho del A2 y no un sorteo (regla 78); y el **175** que el benchmark por deudor cuenta sólo operaciones del tubo con la tasa de cierre registrada (regla 79). Última corrida: **178/178 PASA**.
6. `PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers node tests/e2e/correr.mjs` — **los casos e2e** (`tests/e2e/*.e2e.mjs`, ~8 min): la app con la sesión iniciada (el OTP se lee de la pantalla), el Modo Directorio para tener operaciones sin stream y el detalle abierto en su pestaña. El runner reinicia el estado al empezar cada ARCHIVO (Directorio apagado, filtro «Con línea», sin modal); dentro del archivo los casos se encadenan como los ordenó su autor. Un caso por regla de pantalla, citado en `invariantes.md` como `e2e-<regla>`: `e2e-12-bis-a…e` (la simulación saca de Prospección y el dual), `e2e-13-octies-bis-a/b` (el vacío se ve), `e2e-13-terdecies` (la línea en la cabecera), `e2e-13-quaterdecies` (vaciar la oferta), `e2e-13-sexdecies-a…d` (armar a mano y retirar la última), `e2e-14-a/b/c` (reevaluación explícita; el `c` fija el defecto del pie), `e2e-15` (la bandeja «Solicitudes»), `e2e-15-bis-bis-a/b` (la solicitud cruza de pestaña), `e2e-15-quinquies` (el documento con el registro inyectado), `e2e-15-quater-bis` (el paso de deudores del wizard), `e2e-27-bis` (los rótulos del menú), `e2e-29-a/b` (pesos en el documento), `e2e-30` (el menú «Acciones»), `e2e-31` (Modo Directorio) y `e2e-CRY-01-limite`/`-bajo-limite` (el límite de intentos del OTP). `00_sesion` es el humo del harness.7. Si el cambio toca la UI, regenerar las capturas: `PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers node capturar_pantallas.mjs`. Es lo que consume el export a Figma; si no se regeneran, el export vuelve a mostrar una versión que ya no existe.
7. Si el cambio toca la UI, regenerar las capturas: `PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers node capturar_pantallas.mjs`. Es lo que consume el export a Figma; si no se regeneran, el export vuelve a mostrar una versión que ya no existe.
