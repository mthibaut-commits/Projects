---
type: conocimiento
title: "Reglas — Otorgamiento y atribución"
description: "El motor de otorgamiento (Modelo de Riesgo v1.0), el ruteo (área, nivel), el piso por monto, los umbrales del tenant, O06, áreas/roles/usuarios, vacaciones y reemplazos, la rotación de personas y el contrato físico en O05"
tags: [conocimiento, reglas, dominio]
timestamp: 2026-09-17T15:29:14Z
---

# Otorgamiento y atribución

> Reglas de dominio de NEX, **verbatim** desde el `CLAUDE.md` anterior al 17-09-2026, agrupadas por tema. Se citan por su número (`regla 4`) y **no se renumeran**: el fuente y otros documentos las referencian así. Índice de todas, con qué caso de la suite verifica cada una: [`invariantes.md`](../invariantes.md).
>
> Reglas en este archivo: **4** · **9-bis** · **13-sexies** · **18** · **19** · **30-ter** · **25** · **35** · **48** · **50** · **51** · **52** · **66**.

4. **Motor de otorgamiento = Modelo de Riesgo v1.0:** catálogo C01–C52 / D01–D23 / O01–O04 inyectado en runtime sobre `REGLAS_CLIENTE` (mutación tras el catálogo viejo). **77 reglas (48 C · 23 D · 6 O) · 183 tramos · 133 de excepción · 69 reglas con excepción**, ninguno sin aprobador, ruteados a **riesgo 97 · comercial 29 · operaciones 7** (medido el 14-09-2026 sobre `atribuciones_otorgamiento.json`). **75 son de la política v1.0 y DOS son propias del proceso, las dos de Operaciones**: **O05** (evidencia del contrato de cesión, ver regla 23) y **O06** (monto cedido igual al monto del documento, ver regla 13-sexies). Se cuentan aparte porque el caso 46 mide la cobertura de la política y dejaría de medirla si sumara reglas nuestras al mismo total. **C47–C50 se retiraron el 14-09-2026** tras implementarse el 11-09: eran la cartera del PAR y quedaban **dominadas por C40–C43**, que miden lo mismo a nivel de cliente con umbral `> 0` —si el par tiene un documento reclamado el cliente también, y C40 ya había levantado la excepción—, así que no podían ser la única causa de un visado. El caso 48 fija la decisión al revés: ni las reglas ni sus variables vuelven sin que la suite se caiga. La propiedad que custodiaban —que el motor **no deduce el tipo de regla del prefijo del código**, sino que lo declara la regla (`porDeudor`)— sigue viva y cambió de testigo a D01–D04. **No hay nivel «Comité»** (INC-06 cerrado el 11-09-2026): el Comité de Crédito no es un nivel aparte ni una cuenta del sistema, así que C05 «Línea Cliente Nuevo» dejó de ser la única regla con el nivel escrito a mano y quedó en **Riesgo N5**, la máxima. Ninguna regla declara un nivel fuera de N1..N5. **El MONTO de la operación escala la atribución** (`PISO_ATRIB_MONTO`, INC-05 cerrado el 11-09-2026): son **dos factores** y el requisito es el mayor — el tramo del risk tier mide cuánto se desvía la variable de riesgo, el piso mide cuánto se arriesga si ese desvío resulta cierto. `max`, no suma: sumando, dos factores medianos exigirían más que un factor extremo. Es **piso**, no reemplazo: el nivel del tramo nunca baja por el monto. **Cada columna satura en el tope de su área** —Comercial hasta N3 (Gerente General), Riesgo hasta N5— porque pedirle a un área un nivel que no tiene no exige más: deja la excepción «Sin aprobador definido», que sería un bug de esta tabla disfrazado de control. Los tramos son los de `CFG_TRAMOS` (≤20 / ≤60 / ≤120 / >120 MM), se ven en Configuración › Otorgamiento y viajan en el JSON de atribuciones. Con esto se **retiró el modelo paralelo de causas de desvío** (`MATRIZ_OTORG`, `TIPOS_DESVIO`, `causasDeDeal`, `nivelReqCausa`, `puedeAccionarCausa`, `autorizarCausa`, `otorgarOperacion` y el cómputo muerto de `OtorgamientosView`): decidía por monto con la convención invertida y su cadena de acción estaba **muerta** —las dos props se pasaban y nadie las invocaba—, así que una operación derivada a otorgamiento manual no tenía salida; ahora la libera `otorgamientoCompleto` desde el VISADO. Sobreviven `CFG_TRAMOS`, `gravedadPorMonto` (alimentan el piso) y `requiereOtorgamiento` (rutea la etapa y gatea el tab). Niveles política N1..N5+Comité (**N5 = máxima**). La homologación `nivel = 6−N` **se retiró** (INC-01, 11-09-2026): el nivel que cada regla necesita es CONFIGURACIÓN de la regla y el motor no lo transforma — `NV` quedó como identidad, conservando el nombre para no tocar las 130 llamadas del catálogo. **El ruteo de una excepción es (área, nivel):** la regla declara el ÁREA y su tramo el NIVEL; con ese par se buscan en la lista de usuarios los de esa área con ese nivel **o superior** (INC-03). Una regla **sin área no la aprueba nadie** — un default silencioso escondería una regla mal configurada. `rolDeAreaNivel(area, nivel)` nombra el cargo requerido, y cuando no hay ninguno el motor lo dice con esas palabras: **«Sin aprobador definido»** (`SIN_APROBADOR`), con el requisito (`área · N`) y **la causa**, que son dos y se arreglan en mantenedores distintos — el área no existe en el tenant (Configuración › Áreas) o nadie la tiene en ese nivel o superior (Configuración › Usuarios). Una lista de aprobadores vacía se lee como «todavía no lo miran», cuando en realidad la operación está pegada esperando a alguien que no existe. Un área que **no está en el catálogo** no tiene aprobadores aunque alguien cargue ese nivel.
    - **El motor recibe el PADRÓN antes de cada ejecución** (`padronAprobadores()` → `{areas, usuarios, cargos}`). `puedeAprobarExc`, `aprobadoresExc` y `rolDeAreaNivel` lo toman por parámetro y **no consultan** `USERS`, `ROL_USUARIO`, `ROL_ATRIB` ni `AREAS_CAT`: son datos del TENANT, no del modelo de riesgo, y mientras los leyera por su cuenta no se podía extraer a un servicio sin arrastrar media app. El default es comodidad para los call sites, no dependencia — el caso 44 le inyecta un padrón con un área y usuarios que no existen y comprueba que decide con ése. **Los dos motores corren del lado del SERVIDOR en producción, no en el navegador** — el atacante ES el cliente, así que un motor que decide acá no decide nada: `REGLAS_CLIENTE` se edita desde la consola. Por eso todo lo que deciden entra por parámetro y sale puro. **Medido TRANSITIVAMENTE** con `node auditar_aislamiento.mjs` (12-09-2026) — el cuerpo de una función no basta: `evaluarOtorgItems` no menciona ningún global pero llama a `varsClienteActual`, que lee `SIM_VERSIONS`. **El script no lo hacía y decía que sí**: medía sólo el cuerpo y daba «41 de 64 extraíbles»; con el grafo de llamadas propagado son **19 de 64**, y el informe marca con `←` de qué callee hereda cada lectura, que es dónde hay que ir a arreglarlo. Tenía además un defecto peor: su extractor cortaba una flecha de VARIAS LÍNEAS cuya primera línea tiene los paréntesis balanceados, devolvía cuerpo vacío y la declaraba «nada global» — así `atribEfectiva` y `puedeVerificarFacturas` figuraban limpias leyendo el padrón del tenant. El corte ahora lo manda la **estructura del archivo** (la siguiente declaración de nivel módulo a columna 0), que es la misma guarda que hizo segura la poda. Puras de verdad, cuerpo y cadena: `verifDecision`, `apiVarsCliente`, `verifEvaluar`, `recortarAsignacion`, `catShares`, `spreadMinDeudor`, `validarMutacion`, `aprobacionFormalCliente`. `asignarLineas` tiene el cuerpo limpio pero **arrastra** las tablas precalculadas (`lineasDeCliente`, `paresPorEmisor`, `lf4MetaPorCliente`, `tipoDeudor`) — eso es BATCH, o sea un SELECT en producción, no estado mutable. **Las cuatro cadenas que arrastraban COMMIT o TENANT se cerraron el 12-09-2026** (casos 56–59): `varsClienteActual(deal, versiones)` y `revOtorgActual(deal, versiones)` reciben las versiones y `evaluarOtorgItems(deal, {versiones})` las pasa —antes las ENTRADAS del motor salían de `SIM_VERSIONS` aunque su cuerpo no mencionara ningún global—; `noConfirmada(deal, f, vetadas)` y `estadoCandidata(f, deal, {vetadas})` reciben el veto; `visadoDeal(deal, {visado})`, `otorgBloqueado`, `bloqueoFirmeInfo`, `causaPerdidaDeal` y `otorgamientoCompleto` reciben el visado —y con estado inyectado **se saltan el cache** a propósito, porque `VISADO_CACHE` está indexado por la operación y devolvería la evaluación hecha con otro visado—; y el padrón trae `etiqueta` para que `aprobadoresExc` no lea `USERS`. Un analizador estático no puede distinguir «lee el global» de «cae al global sólo si no le pasan el estado»: eso se prueba inyectando un estado que contradiga al del navegador, que es lo que hacen los casos 56–59. `puedeAprobarExc` y `rolDeAreaNivel` figuran arrastrando TENANT sólo por el default `padron || padronAprobadores()`: con el padrón inyectado son puras. `evaluarOtorgItems` sólo el catálogo en su cuerpo; `puedeAprobarExc`/`aprobadoresExc`/`rolDeAreaNivel` reciben el padrón; **`visadoDealCalc(deal, visado)`** y **`filasVerificacion(deals, {tel, vetadas})`** reciben el estado — el visado y el commit de la verificación son evidencia con actor y hora, o sea del servidor. Lo que sigue atado al navegador y no se arregla con desacoplar: `nowStamp()` dentro de `verifFactura` (la hora la pone el servidor) y `_VERIF_PAR`, que en producción es un SELECT del batch diario. El contrato que el resolver debe implementar ya está escrito en `INVARIANTES` — OTG-01 dice literalmente «el resolver recalcula la atribución desde el rol del token, no desde el payload; la UI que oculta el botón no es el control». El padrón se memoiza **validado por firma** (~18 entradas) y no por un invalidador que haya que acordarse de llamar: la primera versión limpiaba el cache sólo en `guardarRoles`/`guardarAreas`, y un cambio de rol por otro camino seguía viendo aprobadores que ya no correspondían. KNOCKOUT = C30/C31/C32 (TGR judicial/convenios/cuotas) → `rechFirme` → pérdida automática. Re-evaluables definidos por `NO_REEV_CLIENTE`.
    - **AMPLIACIÓN del 23-09-2026 (ADR-0016, regla 70):** el visado sobrevive a la re-evaluación por clave estable, salvo el de la excepción que la versión nueva YA NO LEVANTA: ése se marca «ya no aplica desde la versión N» —no se borra— y no se reactiva si la regla vuelve a levantar.

9-bis. **Un umbral de política se lee con `pol(clave, default)` y no se incrusta en ninguna parte.** Mientras el umbral viva duplicado —una copia declarativa editable en Configuración y una copia literal dentro de la regla— editar la primera no cambia nada, y quien usa el mantenedor una vez y no ve efecto deja de creerle al resto de la pantalla. Cerrado el 14-09-2026 para lo que quedaba: `otrosDeudoresPct` dimensiona el comodín LF4 (era `0,0909` a mano; se pide «% sobre los cupos de PAR», así que sobre el total es `pct/(100+pct)`) y la glosa de la propuesta al comité toma `vigenciaLineaMeses` y `notaMinCompra`, que decía «vigencia de 12 meses» y «nota ≥ 3,7» escritas a mano. **`tasaMinAbsoluta` era peor que muerto:** estaba declarado DOS veces —en la configuración del tenant y en `CFG_ATRIB_DESCUENTO`— y `evalAtribucion` leía la constante, así que bajar el mínimo absoluto en el mantenedor no cambiaba nada y el control seguía rechazando con 0,78; los % de descuento ya se habían migrado y éste quedó atrás. Queda una sola declaración. Y **cablear una perilla obliga a mirar el cache**: `lineasDeCliente` memoiza por RUT, así que mover `otrosDeudoresPct` dejaba el dimensionamiento anterior servido; se valida por **firma** del umbral, como el padrón de aprobadores, y no por un invalidador que haya que acordarse de llamar desde el mantenedor. Las dos perillas que ningún motor consume —`concentracionDeudorPct` en su parte de línea y `frecuenciaMin`— lo dicen en su propio `hint`: son declarativas, viajan en el contrato del servicio y este prototipo no las aplica. Caso 90. **`lineaMinima` se sumó el 15-09-2026** con la misma higiene: entra en la firma de `lineasDeCliente` **y en la de `lineasDeudor`**, que no tenía invalidación ninguna (`if (_deudorIdx) return`) y habría seguido sirviendo el índice anterior — ver regla 27.
    - **CORRECCIÓN del 20-09-2026 (regla 44):** `otrosDeudoresPct` y `lineaMinima` **ya no las consume ningún motor de NEX**. Dimensionar dejó de ser responsabilidad del pipeline —la estructura de líneas llega por el activo A23 y acá sólo se lee—, así que las dos pasan a la categoría DECLARATIVA que esta misma regla define para `concentracionDeudorPct` y `frecuenciaMin`: viajan en el contrato del tenant, las aplica el sistema de gestión de líneas y su `hint` lo dice con todas las letras. Con eso se va también la trampa del cache que este párrafo describe —no hay dimensionamiento que quede servido si no se dimensiona— y la firma de `lineasDeCliente` y `lineasDeudor` desaparece. Lo de arriba fue verdad hasta esa fecha y se conserva porque explica por qué la firma existió. Los casos **90** y **102** cambiaron de significado en el mismo commit: hoy prueban que mover la perilla **no** mueve nada, y lo exigen con la LF4 existiendo, para que «no se movió» no se cumpla sola con dos ceros.
    - **CORRECCIÓN del 23-09-2026 (regla 68):** `frecuenciaMin` dejó de ser declarativa: `intervaloJobMs(cfg)` la convierte en el intervalo del job del inbound en producción, y las horas de reinicio y corte (`horaInicio`, `horaFin`) las consume `jobDelReloj`. Quedan declarativas `concentracionDeudorPct`, `otrosDeudoresPct` y `lineaMinima` (regla 44).

13-sexies. **O06 · El monto cedido tiene que ser el monto del documento — y ese control NO existía** (14-09-2026). El usuario lo planteó como un recuerdo: «me parece que hay una regla en el motor de otorgamiento que revisa que el monto de cesión sea = monto de la factura, en el área de operaciones; esa regla busca detectar estos casos». **No existía, ni en el código ni en la política.** Buscado en el catálogo vigente y en el legado (`Legado/Rules Cliente2_v0.xlsx`, la fuente de las 75): la política v1.0 trae **dos** reglas sobre cesiones y las dos son de **CONCENTRACIÓN** —**C37** «Ratio Venta vs. Monto Cedido», cuánto de su venta cede el cliente, y **C39** «Alta Cesión a Factorings Pequeños», a quién se la cede—, las dos del área **comercial**. Ninguna mira el monto de UN documento, y el área de Operaciones sólo tenía O05. El control faltaba justo donde el dato podía romperse, y sólo se pudo ver al tener cesiones parciales en el archivo: **mientras todas las cesiones eran por el total exacto, ni el hueco de la regla ni el del dato tenían cómo notarse.**
    - **O06 es el par documental de O05**: aquél comprueba que EXISTA la autorización del contrato de cesión, éste que lo cedido COINCIDA con lo que se va a comprar. Los dos son de Operaciones y los dos son nuestros, no de la política.
    - **Dos tramos, a niveles distintos, porque no son el mismo hecho.** Una cesión **parcial** deja el crédito con dos dueños y cobrarlo sería compartir la cobranza: excepción **Operaciones N3** (Jefe de Operaciones). Una cesión por **más** que la factura no es una diferencia sino un crédito que no existe: **Operaciones N5**, el tope del área. Ese segundo caso el activo lo valida en origen y no debería llegar nunca — y justamente por eso el criterio lo mira: **un control que sólo cubre lo que ya sabemos que pasa no controla nada.**
    - La variable se **mide** (`varsOperacionCli` → `cesionParcial` / `cesionExcedida`), documento a documento, sobre las facturas de la oferta contra el registro de cesiones del A2. Caso **96**; el 46 y el 48 pasaron a contar dos reglas propias en vez de una.

18. **Áreas, roles y usuarios: TRES pantallas de Configuración, todo por TENANT.**
    - **Configuración › Áreas** (`AREAS_CAT`, `pc_areas_<tenant>`): las áreas que aprueban excepciones. Existe porque el ruteo es (área, nivel): si un criterio debe ir a un área que este factoring no tiene, se crea acá. El **`id` no se edita** —es lo que el catálogo de criterios compara contra `regla.area`, y renombrarlo dejaría reglas apuntando a un área inexistente—; lo editable es la etiqueta. Las cuatro base (comercial, riesgo, operaciones, verificación) no se eliminan, y una creada tampoco si algún criterio rutea a ella. La tabla muestra cuántos tramos rutean a cada una y **quién la tiene**, con aviso naranja si hay criterios y nadie que los apruebe. `AREA_LBL` se deriva del catálogo.
    - **Configuración › Roles** (`ROLES_CAT`) y **Configuración › Usuarios** (`ROL_USUARIO`). Una sola fuente: **Configuración › Roles** lista el **catálogo** (qué roles existen, qué habilita cada uno, quién lo tiene) y es de LECTURA —el código pregunta por `ejec_verif`, no por una etiqueta, así que crear roles ahí daría filas que no gatean nada—; **Configuración › Usuarios** es donde se **asigna** el rol a cada persona. Diez roles —ejecutivo comercial, jefe de grupo, gerente comercial, gerente general, jefe de riesgo, subgerente de riesgo, operaciones (N5), **jefe de operaciones (N3)**, **ejecutivo de verificación** y super administrador—. El de Operaciones N3 se dio de alta el 13-09-2026 para O05: el área tenía un solo cargo, en N5, así que un requisito N3 se cubría por escalada y no se distinguía del que sí necesita la máxima atribución. **Dar de alta el cargo no basta** — `atribDe` devuelve atribución vacía a quien no figure en `ATRIB_USUARIO`, así que el cargo sin titular deja el par (área, nivel) con nombre y sin nadie que lo firme. Antes el rol venía fusionado en el nombre (`USERS.JG` = «Sofía Herrera · Jefe de Grupo Comercial»), así que un factoring con otra estructura no se podía representar sin editar el código; ahora `nombreDe(code)` da la persona y `rolLabel(code)` el cargo. Se guarda en `pc_roles_<tenant>` con la misma higiene que los permisos (sólo entran usuarios y roles que el catálogo declara) y cada cambio va a auditoría con el actor de la SESIÓN.
    - **LA ATRIBUCIÓN SIGUE AL ROL** (`ROL_ATRIB` → `atribDeRol` → `atribDe`). Antes el nivel estaba cableado por CÓDIGO de usuario —`GG: { atrib: { comercial: 3 } }`— y funcionaba sólo porque había un usuario por rol y el código era su abreviatura; cambiarle el cargo a alguien no le cambiaba la atribución. Ahora `ROL_ATRIB` es el único lugar donde vive qué nivel y qué área implica cada cargo, `ATRIB_USUARIO` queda como padrón (quién existe) y el `tipo` aprobador/pipeline también se deriva. Los niveles del mantenedor de Otorgamiento pasaron a LECTURA: se cambian cambiando el rol.
    - **Decisiones de negocio (11-09-2026), que cerraron INC-02:** aprueba cualquier nivel **igual o superior de la MISMA área**, sin tope. Dos personas con el mismo rol aprueban las dos. Un cargo **vacante** lo cubre la jefatura de su área —un N3 visa lo que le tocaba al N1—, y la escalada **no cruza áreas**: un Gerente General no visa una excepción de Riesgo. Se implementó eliminando `nivelesAprobArea` y dejando `puedeAprobarExc` en `lv >= nivelReq`.
    - **INC-01 e INC-03 también quedaron cerrados** (11-09-2026, ver regla 4). Verificado: C01 «Existencia de Pagaré Firmado» pasó de exigir al Subgerente de Riesgo a exigir **Operaciones N1**, y C21 «Deuda Morosa ACHEF +180» del Jefe de Grupo Comercial al **Subgerente de Riesgo**. Ningún par (área, nivel) del catálogo queda sin aprobador. **Observación de configuración:** Riesgo sólo tiene cargos en N4 y N5, así que sus 78 tramos de N1–N4 caen todos en el Jefe de Riesgo por escalada; distinguirlos es dar de alta usuarios de Riesgo en N1–N3, no tocar código.
    - Lo único que el rol gatea hoy es `puedeVerificarFacturas`: **sólo el Ejecutivo de verificación** (y el super-admin) marca una factura como verificada o no verificada, en la mesa y en el tab del detalle. El resto ve el estado pero no lo firma — una verificación es evidencia de una llamada. `EV` (Camila Soto) viene con ese rol para que sea probable.

19. **Vacaciones y reemplazos** (`Configuración › Vacaciones y reemplazos`, `pc_reemplazos_<tenant>`). Mientras alguien está fuera, quien lo cubre **asume sus atribuciones**: el mismo nivel de la misma área, sólo durante el período.
    - **Entra por el PADRÓN, no por el motor.** `padronAprobadores(hoy, reemplazos)` empaqueta quién puede aprobar qué, y un reemplazo es exactamente eso; `puedeAprobarExc`, `aprobadoresExc` y `rolDeAreaNivel` lo respetan sin enterarse de que existe. Meterlo dentro del motor habría metido una política de RRHH en el modelo de riesgo. El fold toma el **MAYOR nivel por área** —cubrir a un N1 nunca baja a un N2— y marca las dos puntas (`cubre` en quien reemplaza, `ausente` en quien está fuera) porque la bitácora tiene que poder decir POR QUÉ esta persona pudo aprobar esto.
    - **Es ADITIVO por defecto, y revocable por reemplazo** (decisión del usuario, 12-09-2026: «que en vacaciones el de vacaciones pueda seguir aprobando desde la playa»). El reemplazo **agrega** un aprobador, no cambia uno por otro: estar de vacaciones no es estar desconectado y un gerente que entra a visar algo urgente no debería toparse con un permiso revocado. El flag `ausenteAprueba` lo desactiva caso a caso, para la ausencia que sí tiene que ser total —licencia, salida— donde dejar la atribución viva es justo el riesgo. **El orden importa:** se le quita DESPUÉS de pasársela a quien cubre, o el reemplazante heredaría un cargo ya vaciado. Una fila sin el campo se lee como «sigue aprobando», así que migrar no le cambia el permiso a nadie. El ausente revocado **no desaparece del padrón** (entra por `u.ausente`): sin eso la pantalla no puede decir por qué dejó de poder aprobar.
    - **`atribDe` ≠ `atribEfectiva`.** `atribDe(code)` es lo que implica su CARGO —lo que se configura y lo que se delega—; `atribEfectiva(code, hoy, lista)` es lo que puede aprobar HOY, y sale del padrón, que es el único sitio donde se aplica el reemplazo. **La UI tiene que mirar la efectiva**: el primer intento tenía el motor correcto y la pantalla no —el badge de Otorgamientos descartaba al reemplazante por «tipo pipeline» antes de preguntarle al padrón—, y una atribución que sólo existe en el motor no existe, porque nadie llega a ejercerla. Lo mismo con `esJefeComercial`/`esGerenteComercial`.
    - **Firmar una verificación también se delega** (`puedeVerificarFacturas(code, hoy, lista)`): no es un nivel sino un rol, pero es una atribución — si la única Ejecutiva de verificación se va de vacaciones, sin esto nadie registra una llamada y las operaciones se pegan en el giro. Por eso el selector «quién se ausenta» lista a quien tiene **nivel o firma** que delegar, no sólo nivel.
    - **La bitácora lo estampa sola** (`_reemplazoDelActor` dentro de `registrarAuditoria`), no en cada call site: son ~100 sitios y el que se olvide es justamente el que después nadie puede auditar. Deja dos formas del mismo hecho, a propósito — el campo `reemplazoDe` es lo que se consulta y la glosa («Acción ejecutada bajo configuración de REEMPLAZANTE de …») es lo que se lee. Sólo se estampa cuando el actor del evento ES el usuario de la sesión: las acciones del sistema o del cliente no se atribuyen a nadie que esté cubriendo a otro. `actorEtiqueta(code, hoy, lista)` es lo que firma además el visado y la verificación, para que los tres digan lo mismo.
    - **El padrón se invalida solo:** la firma de memoización incluye los reemplazos **y la fecha**, así que un reemplazo que empieza hoy cambia quién aprueba sin que cambien roles ni áreas. Higiene del storage como roles y áreas: sólo entran filas con dos usuarios que el catálogo declara, distintos, y fechas ISO bien ordenadas. Se valida el solape: dos personas cubriendo a la misma en el mismo día dejarían la atribución duplicada y la bitácora sin poder decir quién la ejerció.

30-ter. **El contrato físico se carga en la tarjeta de O05, y solicitar no cierra la puerta** (16-09-2026, pregunta del usuario: «¿cuando elijo firmar físicamente, dónde cargo el contrato?»). Las dos mitades de la pregunta tenían la misma causa: **el sitio donde se actúa no decía nada**.
    - **DÓNDE.** El criterio **O05 · Contrato firmado por cliente de la operación** ya existía y la vía física ya lo dejaba abierto (regla 23), pero eso lo decían el modal de curse —al confirmar, o sea antes— y el badge de la oferta publicada, que están en **otra pestaña** del detalle. En la tarjeta del criterio, que es donde se adjunta, no lo decía nadie: había un botón «Solicitar aprobación» y, dentro, un «📎 Adjuntar archivos» que no se anuncia como el sitio del contrato. Ahora la tarjeta lo dice con esas palabras, y el cargo que autoriza se **deriva** (`rolDeAreaNivel`) en vez de escribirse: quién firma en Operaciones lo configura el tenant (regla 18). El texto es distinto según la vía —en la electrónica el criterio lo cierra la firma del cliente y no hay nada que adjuntar— porque un aviso que pide un papel cuando no hace falta enseña a ignorarlo. Desaparece al visarse: ahí la evidencia ya existe.
    - **POR QUÉ NO SE PODÍA.** `if (sol) return solBlock` dejaba el panel en **sólo lectura** apenas se enviaba la solicitud, y lo que llega después —el contrato firmado que aparece dos días más tarde, la aclaración que el apoderado pidió por teléfono— no tenía por dónde entrar: la única salida era esperar. **Aportar respaldo no decide nada**; lo que está gateado por atribución es VISAR, y eso sigue igual. Se suma además al camino en bloque: «Marcar sin comentarios y solicitar (N)» manda las 47 de una vez, y antes eso **congelaba las 47** sin que ninguna admitiera un antecedente después.
    - **`ampliarSolicitudExc` APILA, no reemplaza** (`ampliaciones: […]`, append-only). La justificación original es evidencia con actor y hora y el apoderado pudo haberla leído ya: pisarla borraría sobre qué se estaba decidiendo. Cada ampliación lleva **su** quién y **su** cuándo —con una sola fecha para todo el bloque, la bitácora no puede decir qué se sabía en cada momento— y firma con `actorEtiqueta`, como el visado y la verificación, para que un reemplazante quede nombrado (regla 19). Va en el **mismo formato de fecha** que la solicitud y el visado y no en el de la bitácora: quedan una encima de la otra en el panel, y con milisegundos en una sola se leen como dos clases de dato distintas.
    - **Sin solicitud previa no escribe nada.** Una ampliación es información PARA alguien; fabricar ahí una solicitud saltaría el aviso y la tarea que genera `solicitarAprobacionExc`, o sea el apoderado tendría el respaldo y ningún motivo para mirarlo. Y una **vacía** tampoco: un bloque con actor y hora y nada adentro anuncia algo nuevo que leer cuando no lo hay.
    - **Lo verifica ABRIR el tab Otorgamiento.** `tsc`, duplicados, build y la suite pasaron los cuatro antes de comprobar nada —no montan `DealDrawer`— y de hecho el primer intento usaba `<Paperclip>` de lucide, **que no está importado**: un componente no declarado revienta en el navegador y en ninguno de los cuatro. Medido en el DOM, de punta a punta: oferta cerrada **en papel**, la tarjeta de O05 diciendo dónde va el contrato, el PDF adjuntado después de la solicitud, la ampliación apilada con su nombre y su hora, la justificación original intacta y el botón disponible para una segunda vuelta. 0 errores de página. Caso **114**.

25. **Rotación de personas: qué ve cada uno y a quién se le atribuye lo hecho** (13-09-2026). El archivo de cartera de cada mañana le asigna al ejecutivo nuevo las **empresas** del anterior, pero no las **operaciones**: `deal.exec` guarda las iniciales congeladas en el JSON de la oportunidad y es lo que decide quién la ve.
    - **`Configuración › Oportunidades › Migración › Cambio de ejecutivo`** hace el traspaso explícito: quién sale, quién entra, y las operaciones se mueven. Es un acto administrativo con fecha y responsable, no un efecto colateral de un archivo, así que va a la bitácora con el detalle. **Sólo se traspasa lo que está EN GESTIÓN, hasta antes del giro** — una operación girada ya se desembolsó y moverla sólo reescribiría de quién cuelga una venta que hizo otro; no es configurable a propósito. La etapa se vuelve a mirar en la mutación, no sólo en la pantalla que armó la lista: entre abrir el mantenedor y confirmar, una operación pudo girarse. **`execOriginal` conserva quién la originó** y se escribe UNA sola vez: en un segundo traspaso el original sigue siendo el primero.
    - **La estructura comercial y la cartera son un ACTIVO: `A24 · Cartera comercial`** (14-09-2026, `Integraciones/spec_s3_cartera.md`). Quién es ejecutivo, de qué equipo, bajo qué **jefatura**, en qué zona y sucursal; y de quién es cada cliente. Nada de eso lo produce el pipeline: es de RRHH y de la administración comercial y llega en el archivo de cada mañana. Vivía en **cuatro constantes de módulo** (`EXECS`, `EXEC_JEFATURA`, `EXEC_ZONA`, `EXEC_SUCURSAL`), un mapa de jefaturas de una línea (`EQUIPO_JEFATURA`) y —la asignación— **dentro del A5**, el activo de Share of Wallet, en un campo `Ejecutivo` llaveado por **NOMBRE**: renombrar a una persona dejaba a toda su cartera sin dueño, sin error y sin forma de notarlo. Ahora los cuatro catálogos son un índice sobre el activo. Tres decisiones: **el código es la identidad** (es lo que se congela en `deal.exec`); **el rótulo del equipo no es clave foránea** —`EQUIPO` se muestra, `COD_JEFE` decide, y por eso renombrar un equipo ya no cambia quién ve qué—; y **los dos granos viajan en el mismo archivo** (columna `TIPO`) porque se validan como una unidad: una asignación a un código que el archivo no declara no se carga, o quedan operaciones colgando de alguien que no existe. Un RUT que no está en el archivo es un **prospecto**, no un cliente sin ejecutivo: quién lo trabaja lo decide el pipeline, no el dato. Caso 91.
    - **El alcance de una jefatura se DERIVA del equipo, no se cablea.** `JEFE_A_EXECS = { JG: ["CR","RF","JT"] }` tenía dos problemas: no coincidía con los equipos que declara la cartera (CR y RF son Andes, JT es Pacífico), o sea había dos verdades sobre quién manda a quién; y un jefe **nuevo** no estaba en el mapa, la búsqueda daba `undefined` y eso se leía como «ve todo». El ejecutivo nuevo fallaba **cerrado** y el jefe nuevo fallaba **abierto**, indistinguible de la gerencia. Ahora el grupo sale de `EXEC_JEFATURA` (que en producción viene del archivo diario), el «ve todo» se decide por **rol** (`ROLES_VEN_TODO`) y lo desconocido devuelve `[]`: en una pantalla de oportunidades ajenas, fallar cerrado es la única opción defendible.
    - **Un código que el padrón ya no conoce NO es el «Agente IA»** (`nombreEjec`). Ese fallback relabelaba en silencio las operaciones de quien se fue, y el dashboard, el Plan por Ejecutivo y el churn empezaban a contarle al agente operaciones que negoció una persona. «Agente IA» queda sólo para lo que de verdad no tiene dueño (`exec` vacío); un código desconocido se muestra marcado, que es trazable y no miente. Lo mismo con `jefaturaOf` e «Inbound / IA».
    - **Una tarea de aprobación es para quien tenga la ATRIBUCIÓN, no para la foto de nombres del día en que se creó** (`destinatariosTarea`). La tarea guarda el par **(área, nivel)** y la lista se resuelve cada vez que se mira, así que sigue al padrón: el apoderado que llega la ve y el que se fue deja de verla, sin migrar nada. El super-admin queda fuera —cubre todas las áreas en el nivel máximo, aparecería en todas las tareas y taparía el caso «no hay a quién pedírselo»—. Y **OTG-01 se comprueba antes de ESCRIBIR** el visado, no sólo al dibujar el botón: la pantalla puede venir de una sesión vieja, de un rol que cambió o de una atribución revocada, y la decisión de un apoderado es evidencia regulatoria.

35. **Una regla MAL DEFINIDA no se ejecuta ni se verifica, y la salida lo dice** (18-09-2026, pedido del usuario sobre el spec del motor: «en caso de encontrar una regla con un criterio sin área, esa regla no deberá ejecutarse ni verificarse; se deberá mostrar en la salida que la regla no se ejecutó ya que no se ha definido correctamente»).
    - **Qué la hace mal definida**: la regla tiene **al menos un tramo de `excepcion`** y **ese tramo no llega a nadie**. El ruteo de una excepción es el par (ÁREA, NIVEL) —la regla pone el área, su tramo el nivel— y con ese par se busca a la **persona** que puede firmarla. Son **TRES causas**, y las tres paran la regla (ampliación del usuario el 18-09-2026 sobre la primera versión, que sólo paraba la primera: «si la regla especifica que es excepcionable debe gatillar el mensaje que está mal definido»):
        - `sin_area` — no declara `area` (`""` cuenta como no declararla). Se arregla en el **catálogo de otorgamiento**.
        - `area_inexistente` — declara un área que este tenant no tiene. Se arregla en **Configuración › Áreas**.
        - `sin_usuario` — el área existe y **nadie la tiene** en el nivel que el tramo pide ni en uno superior. Se arregla en **Configuración › Usuarios**.
      **«Nadie» es nadie de la escalera de atribución.** El **super-admin** puede visar cualquier criterio —`puedeAprobarExc` lo deja pasar antes de mirar el área— y eso **no cuenta**: es la llave maestra del tenant, no el aprobador que la política designa. Con el mismo criterio, una regla sin área tampoco se ejecuta aunque el super-admin pudiera firmarla, que es lo que ya se decidió el 18-09-2026 por la mañana. La tabla de `Atribuciones` lo sigue listando con su paréntesis «(ve todo)» y eso está bien: dice quién podría destrabarlo a mano, sobre el recuadro naranjo que dice que la regla no corrió.
      Lo único que le importa a la operación es si hay alguien al otro lado; la causa se guarda igual porque **cada una se arregla en un mantenedor distinto**, y mandar al usuario al equivocado es casi tan malo como no decirle nada. Se prueban **TODOS** los tramos de excepción, no el primero: cualquiera puede ser el que dispare, y uno solo sin destinatario deja la operación pegada igual.
    - **La compuerta necesita el PADRÓN, y le entra por parámetro**. `reglaNoEjecutable(regla, padron)` juzga si la regla es *routeable en este tenant*, así que ya no basta con su texto —una regla intacta pasa a estar mal definida el día que alguien borra un área en Configuración—. Pero **no lo lee por su cuenta**: quien itera el catálogo (`evaluarOtorgItems`, `snapVersionCli` y las tres pantallas de mantenedor) lo calcula UNA vez y lo inyecta. El primer intento llamaba a `rolDeAreaNivel`, que se busca el padrón cuando no se lo pasan, y `auditar_aislamiento.mjs` —que sigue las llamadas— sacó a `evalReglaCli` de las funciones puras en el acto: por eso el núcleo se partió en `cargoDeAreaNivel(area, nivel, pad)` (puro, exige el padrón) y `rolDeAreaNivel` quedó como su adaptador para las pantallas. Sin padrón la compuerta **falla CERRADO** (`causa: "sin_padron"`): quien olvide inyectarlo ve el criterio marcado en pantalla, no aprobado en silencio.
    - **NO todas las reglas necesitan aprobador** (corrección del usuario el mismo día, contra el primer intento, que exigía área a toda regla con tramos). El área es a quién se le pide la **excepción**; una **knock out** —tramos sólo de `rechazado`— no se aprueba: incumple y se acabó, no hay a quién pedirle nada. Exigirle área la habría dejado **sin ejecutar por una carencia que no lo es**, que es el defecto opuesto al que esta regla cierra. Lo mismo las de **clasificación** (informan) y las **sin tramos** (no deciden). Es el **MISMO criterio** con que la mesa de reglas arma su lista (`tiers.some((t) => t[1] === "excepcion")`): si divergieran, la mesa mostraría reglas que el motor no rutea, o al revés — y el caso 141 lo comprueba recorriendo el catálogo real, en las dos direcciones.
    - **Qué pasa entonces**: `evalReglaCli` **corta antes de mirar los tramos** y devuelve la quinta disposición, `no_ejecutada`, con el **motivo** y **dónde se arregla**. No entra en `exc` ni en `rech` —no se evaluó, así que no puede concluir nada— y por eso **no bloquea**: lo que protege a la operación no es el bloqueo sino que el problema se VEA.
    - **NUNCA EN SILENCIO** (el usuario, dos veces el mismo día: «nunca puede pasar en silencio si está mal definida» y «si la regla está mal definida, debes mostrarlo en la pantalla»). Se ve en **ocho** sitios; los cinco últimos se agregaron porque por ahí la operación —o la regla misma— se veía LIMPIA. Y el método para encontrarlos fue **capturar la pantalla con una regla plantada**: el fuente no dice qué se esconde, la captura sí.
        - La **fila del criterio**: badge ámbar «No ejecutada · falta configuración» y un recuadro que dice «Esta regla no se ejecutó ni se verificó… La operación se evaluó SIN ella», con la causa y el mantenedor. El **hallazgo** de la regla **no** se muestra ahí: afirmaría algo que nadie midió.
        - Un **aviso de cabecera por tab** («⚠ N regla(s) NO se ejecutaron») y su **balde propio**, nunca dentro del acordeón de las aprobadas.
        - El **contador del pie del panel** y el **tooltip de la compuerta** de Otorgamiento.
        - La **tarjeta del Kanban**: devolvía `null` —una regla no ejecutada no llega a `exc` ni a `rechReev`— y la operación se veía limpia en el tubo, que es donde se mira sin abrir nada. Ahora lleva su badge ámbar «N criterio(s) sin ejecutar» con el detalle en el tooltip.
        - El **denominador «N/M criterios» del tubo**: el criterio que no se ejecutó desaparecía del total —«16/321» pasaba a «16/320»— y encoger el denominador es la forma más silenciosa de todas. Ahora cuenta.
        - `Configuración › Otorgamiento › Atribuciones de aprobación`, que es **donde se arregla**: mostraba un chip de área VACÍO (`AREA_LBL[undefined]`) y nada más. Ahora la fila va en naranjo con «Sin área · NO SE EJECUTA» y la frase completa.
        - `Configuración › Otorgamiento › Criterios de verificación`, **la vía más silenciosa de todas y la que destapó una captura**: esa pantalla agrupa por área (`areas = ["operaciones","comercial","riesgo","extras"]`, `r.area === area`), así que una regla sin área **no pertenece a ningún grupo y desaparece de la pantalla que la cataloga** —no hay nada que mirar mal, simplemente no está—, mientras la bajada seguía diciendo «las 78 reglas del cliente». Ahora las que no caen en ningún grupo van **primero**, en su propio bloque naranjo, y la bajada dice cuántas son. Sirve además para un área que el tenant cree y esta lista fija no agrupe: ésas sí se ejecutan y se marcan distinto.
        - `Configuración › Áreas`, **donde se declaran**: su tabla cuenta «Criterios que rutean acá» por fila, y una regla sin área no rutea a ninguna, así que la suma dejaba de cuadrar con el catálogo sin que nada lo dijera. Ahora un recuadro naranjo arriba los lista **uno por uno con su número**, porque lo que hay que hacer es ir a buscarlos.
    - **La compuerta va en `evalReglaCli`**, que es el único sitio por donde pasan las reglas de cliente y las de deudor. En cada consumidor, el que se olvidara la evaluaría igual — que es exactamente cómo el `disp` de una regla sin área terminaba pareciendo un veredicto.
    - **Dónde sigue viviendo «Sin aprobador definido»** (spec de excepciones §6.4): en el **piso por monto**. `nivelExigido` sube el nivel que el tramo pide según cuánto se está girando, y ese nivel es de la OPERACIÓN, no de la definición de la regla —la misma regla rutea bien en una operación chica y pide más arriba en una grande—, así que ahí no corresponde no ejecutarla: corresponde decir que a ese monto no hay quien firme. El caso **45** ya fija que el piso nunca pide más de lo que su área alcanza. Fuera de eso, el mensaje dejó de ser la respuesta a un área sin gente: esa regla ahora no se ejecuta.
    - **Hoy ninguna regla del catálogo cae acá**: las 77 declaran área y las 69 con excepción tienen a quién pedírsela en este tenant —medido, no supuesto: riesgo N1–N3 escalan al Jefe de Riesgo (N4) y operaciones N1–N2 al Jefe de Operaciones (N3)—. Así que el caso se prueba **plantando** la regla: un gate que compare el catálogo consigo mismo pasaría siempre y no vigilaría nada. El caso 143 deja la medición adentro, de modo que el día que alguien borre un área o la deje sin gente **se rompe**, que es el punto.

48. **EL ATAJO DEL OTORGAMIENTO AUTOMÁTICO NO PASA POR ENCIMA DEL VISADO (OTG-02)** (21-09-2026,
    reportado por el usuario: «esto está rarísimo, por qué hay deudores en la operación con
    requerimientos de otorgamiento», sobre una pantalla que mostraba el cartel verde «Otorgamiento
    automático · se otorga automáticamente, sin intervención de un especialista» con
    «**Criterios por aprobar 38**» a un centímetro, el tab rotulado «Otorgamiento **38**» y chips
    «Otorg. 3/23» en los deudores).
    - **Son dos cosas distintas y el código las trataba como una.** `requiereOtorgamiento` es una
      heurística **anterior al motor de reglas**: mira si hay deudores «Otro» y si se supera la línea, y
      nada más. Responde «¿hace falta que un especialista la mire *por línea o por deudor*?». El
      **visado** evalúa las 77 reglas del CLIENTE —CMF, TGR, Equifax, concentración— y no tiene nada que
      ver con la lista del deudor: una operación de puros deudores Prime, dentro de línea, puede tener
      decenas de criterios esperando excepción. `otorgAuto` no dice que el visado no exista.
    - **Lo grave no era el cartel sino el avance.** `otorgamientoCompleto` abría con
      `if (deal.otorgAuto) return true;` —antes de mirar el visado— y la rama periódica del atajo sólo
      comprobaba las llamadas (`verifResumenDeal(d).pend`), así que una operación con 38 criterios sin
      aprobar llegaba a «Pendiente Integración · esperando a Operaciones» con el otorgamiento sin hacer.
      **OTG-02 estaba declarado en `INVARIANTES` y el código no lo obedecía** por este camino: «con
      excepciones o rechazos re-evaluables sin resolver, la operación no puede pasar a Cesión».
    - **Es la misma lección que la regla de la firma**, un nivel más abajo: *las compuertas mandan sobre
      el atajo*. Cuando se cerró en el momento de firmar (caso 88) quedó viva en el avance periódico,
      que es el que mueve la operación cuando nadie está mirando.
    - **El arreglo**: `otorgAutoVigente(deal, estado)` —puro, con el estado por parámetro— responde si el
      atajo sigue en pie: `otorgAuto` **y** el visado sin `excPend` ni `rechReev`. `otorgamientoCompleto`
      comprueba OTG-02 **antes** del atajo, y las dos ramas del avance —automática y manual— se fundieron
      en **una sola**: lo único que cambiaba entre ellas era la glosa, y separadas volverían a separarse.
    - **En la pantalla**: el cartel verde sale sólo con el atajo vigente; si no, se muestra la lista de
      criterios, que antes quedaba escondida detrás de `!deal.otorgAuto` — el peor de los dos mundos,
      porque la operación decía «38 criterios por aprobar» en el resumen y el tab que los muestra estaba
      reemplazado por un cartel diciendo que no hacía falta nadie.
    - **Queda pendiente, anotado y no tocado**: el sub-estado del tubo (`subEstadoDe`) sigue rotulando
      «Automático» mirando sólo `d.otorgAuto`. Es la misma contradicción en chico; se corrige con su
      propio caso porque esos rótulos están gateados (casos 110, 111).
    - Caso **153**, en las dos direcciones —con criterios pendientes y sin ellos, en la automática y en
      la manual—. Gate de forma: `regla_48.test.mjs`.

50. **El cierre del negocio le escribe a quien tiene que firmar, y el remitente es el sistema** (21-09-2026, reportado por el usuario: «porque cuando se cierra un negocio no se están enviando los mensajes a los responsables ni al ejecutivo que tienen responsabilidad de aprobar. Además en el menú otorgamiento tampoco cuando cambias a uno de los aprobadores no se lista nada»). Tenía razón, y el defecto era una **ausencia**: `confirmarCierre` —162 líneas, el handler de la firma del cliente en el portal— no llamaba a `hiloEnviar` ni a `hiloNuevo` **una sola vez**. Una ausencia no la caza ningún test que mire la salida, porque no hay salida que mirar; por eso esta regla se gatea sobre el TEXTO del fuente.
    - **Por qué se notaba tan poco: de las DOS puertas a la mesa de otorgamiento, sólo avisaba una.** La manual —el ejecutivo aprieta «Pre-evaluación»— llama a `avisarPreEval` desde el 13-09. La automática —el cliente firma y, como dice el comentario del propio botón, «la bandeja de otorgamiento la toma sin que nadie la envíe»— no llamaba a nada. O sea que **justo el camino donde nadie aprieta un botón era el que no le escribía a nadie**, y la operación quedaba esperando una firma que sus firmantes no sabían que existía.
    - **Qué manda el aviso** (`avisarCierreNegocio`, nivel de módulo): un hilo de la mensajería por operación, asunto `Cierre de negocio · <id>`, con el **ejecutivo dueño** y **los aprobadores de cada excepción pendiente** como participantes. El texto dice el negocio, el cliente, el monto firmado, **cuántos criterios faltan agrupados por (área, nivel) con quién firma cada tramo** —«2 de Comercial N3 (Federico Diaz · Gerente General) · 1 de Riesgo N5 (Paula Reyes · Subgerente de Riesgo)»— y cuántas facturas esperan la verificación telefónica. Un número suelto («3 criterios») no le sirve a nadie para saber si le toca.
    - **El remitente es el SISTEMA (`CODE_SISTEMA`), no el ejecutivo.** La firma la hizo el CLIENTE en el portal de Factoring Security; atribuirle al ejecutivo un mensaje que no escribió sería falsear la bitácora, y hay algo peor: `hiloEnviar` marca leído **sólo al remitente**, así que el aviso le habría llegado ya leído justo a quien el usuario dijo que no le llega nada. `SIS` **no entra a `USERS`** —sería un login sin persona, con selector de sesión y rol asignable— y **no se suma a `participantes`**, que son códigos de usuario que cinco pantallas resuelven contra `USERS`; queda en el mensaje (`de`/`deNombre`), que es donde corresponde. Lo resuelve `nombreEnHilo`.
    - **Calla cuando no hay nada que firmar ni que verificar.** Un mensaje «no tienes nada que hacer» en cada operación cursada limpia vacía la campana de significado, y ésas son la mayoría. El caso 152 prueba las **dos direcciones**, que es lo que `testing.md` pide de un control.
    - **Los destinatarios salen del PADRÓN, no de `ATRIB_USUARIO`** (`codigosAprobadoresDe`, compartida con `avisarPreEval`). `aprobadoresExc` devuelve ETIQUETAS —sirve para pintar «Aprueban: …»— y escribirle a una etiqueta no le llega a nadie. El padrón además ya aplica los **reemplazos por vacaciones** (regla 19): el bucle a mano que tenía `avisarPreEval` recorría `ATRIB_USUARIO`, que es sólo quién existe, así que **quien cubre a un ausente no recibía el aviso**. El **super-administrador queda fuera**: puede firmar todo, o sea que estaría en cada hilo del sistema.
    - **El aviso se arma FUERA del updater de `setDeals`**, igual que el patch de `cerrarOferta` (regla 22): React no ejecuta el updater en el acto y puede llamarlo más de una vez, así que un envío ahí adentro sale duplicado. Se evalúa el paquete **FIRMADO** y no el ofertado —las excepciones de una operación en cesión no son las mismas que las de la oferta—, con `dealFirmado`/`montoFirmado`, que son **una sola expresión** usada por el updater y por el aviso: dos copias se desfasan sin que nada lo diga, que es el patrón de VER-01.
    - **El hilo cruza de pestaña** (`recibirHilo` + `nex-hilo`). La firma vuelve del portal a la pestaña que lo abrió, y ésa puede ser la del **detalle**, que monta el `DealDrawer` y nada más: sin campana ni bandeja de mensajes. Sin el puente, el mensaje existía en la memoria de un documento que no lo muestra — el mismo agujero que ya cerraron `nex-solicitud` y `nex-preeval` (regla 15-bis-bis). Se identifica por **(operación, asunto)** y no por `h.id`, que se numera con el largo de la lista LOCAL y colisiona entre pestañas: dos hilos distintos pueden ser los dos `H1001`.
    - **La segunda mitad del reporte no era un defecto del motor, era el cartel.** Medido sobre la mesa de Otorgamientos: **180 excepciones pendientes** en 5 operaciones —110 de comercial N3, 58 de riesgo N5, 12 de operaciones N4— y sólo **tres cargos** las alcanzan (Gerente General, Subgerente de Riesgo y Operaciones, más el super-admin). Jefe de Grupo Comercial, Gerente Comercial, Jefe de Riesgo y Jefe de Operaciones ven **cero**, y eso es **correcto**: `ROL_ATRIB` da un área y un nivel por rol y `puedeAprobarExc` exige nivel ≥ el requerido **dentro de la misma área** (INC-03). Lo que estaba mal era que la bandeja vacía decía «No tienes operaciones con acciones pendientes», que se lee como «no hay nada que hacer» cuando hay 180 cosas que hacer y ninguna es tuya. Ahora distingue las dos causas, lista los tramos con quién firma cada uno y nombra la atribución propia del que mira.

51. **El estado del otorgamiento cruza de pestaña, y lo hace con LAS DOS COSAS: storage y aviso** (21-09-2026, reportado por el usuario en tres formas distintas antes de que se viera que eran la misma: «cuando el Gerente Comercial ingresa al otorgamiento no le aparece ninguna línea pendiente siendo que en el detalle de otorgamiento sí se requieren de su aprobación», «tampoco se gatilló el mensaje en esta operación a los involucrados», y antes «el ejecutivo de verificación no puede accionar las acciones desde el menú verificación de las operaciones que fueron enviadas al comité y/o con solicitud de pre-evaluación»). **Un solo defecto, tres síntomas.**
    - **La causa.** `PRE_EVAL` era un `let {}` de módulo y `HILOS` un `let []`. El **detalle es pestaña propia** (regla 15-bis-bis) y un `let` de módulo es memoria de CADA documento: lo que el ejecutivo hacía en el detalle no existía para la pestaña del tubo, que es donde viven **la mesa de Otorgamientos** y **el Centro de mensajería**. `solicitarAprobacionExc` llama a `setPreEval` y a `hiloEnviar`, así que **las dos cosas se perdían a la vez**, y `excEnBandeja` —que decide si una operación se puede VISAR— consulta `tienePreEval`: el aprobador veía la bandeja en cero mientras el detalle listaba sus criterios con su nombre.
    - **Sólo el BOTÓN «Pre-evaluación» avisaba.** Tenía su `avisarOpenerPreEval` escrito a mano junto al `onClick`, y el otro camino —solicitar la aprobación de UNA excepción, que es el que el ejecutivo usa de verdad— no lo tenía. Esa asimetría es exactamente el modo de falla que la regla 50 documentó en el cierre: cuando el aviso vive en el call site, el call site que se escribe después se olvida. Ahora **difunde `setPreEval`**, que es el punto único por donde pasa el estado.
    - **Hacen falta las DOS y no una.** El **storage** (repositorio) hace que el estado sobreviva a cerrar la pestaña; el **postMessage** hace que la pestaña que YA está abierta se entere. El aprobador tiene el tubo abierto mientras el ejecutivo trabaja en el detalle, así que sin el aviso no se entera hasta recargar; y el aviso solo no sirve porque la pestaña se cierra. Es la misma distinción que `crearRepo` ya tenía escrita («aquél cruzaba a una pestaña ABIERTA y esto tiene que sobrevivir a que la pestaña se cierre») y que faltaba aplicar acá. **`BroadcastChannel` y el evento `storage` no son opción**: con `file://` cada página es un origen único y no se hablan (ADR-0012).
    - **Releer no es gratis y por eso es explícito.** `crearRepo` carga el storage UNA vez al montar el módulo, así que una pestaña abierta no ve lo que otra escribió ni en los repositorios que ya existían —visado, solicitudes de excepción—. `repo.recargar()` vacía y rellena el MISMO objeto `datos` (los alias `VISADO_STATE`/`SOLICITUD_EXC`/… apuntan a su tabla, así que reasignarlo los dejaría mirando la tabla vieja), y `refrescarEstadoOtorgamiento()` lo corre sobre los seis repositorios del otorgamiento e invalida el visado memoizado.
    - **El receptor no re-difunde.** `setPreEval(dealId, code, on, difundir = false)` es lo que llama el handler del mensaje: sin ese corte, dos pestañas que se tengan la una a la otra como opener se rebotan el aviso.
    - **Los `id` de hilo dejan de colisionar.** Se numeran con el largo de la lista local (`"H" + (HILOS.length + 1001)`), así que dos pestañas producían dos «H1001» distintos. Guardar la **lista completa** bajo una clave —y no un hilo por clave— hace que las dos partan de la misma lista y numeren igual; `recibirHilo` identifica igual por **(operación, asunto)**, que es lo único estable entre documentos.

52. **El TENANT se da de alta en la plataforma, y un tenant sin administrador no lo puede usar nadie** (21-09-2026, pedido del usuario: «implementa en la configuración un menú Tenant en donde se cree el Tenant de Security y/o otro cliente; saca la configuración del logo y de los colores y déjalos en ese menú; en ese menú también deberías poder crear al admin del Tenant para que pueda ingresar y empezar a crear a los otros usuarios»).
    - **La lista de factorings es de la PLATAFORMA, no de un tenant.** `TENANTS` era un literal de un elemento; ahora se hidrata de `nex_tenants`, y esa clave **no lleva sufijo** a propósito: `pc_roles_security` es «los roles DE Security», pero guardar la lista de factorings por tenant sería que cada uno tuviera su propia idea de quién existe. En producción es la tabla `tenant` y el alta la hace la plataforma. Misma higiene que roles, áreas y etapas —sólo entra lo que tiene la forma declarada— y con un motivo extra: el `id` **compone las claves de storage** de toda su configuración, así que un id inválido no es una fila rara, es un prefijo roto. El tenant base nunca se pierde: es el que resuelve el fallback de `resolverTenant`.
    - **El orden importa y es lo que esta pantalla enseña:** crear el tenant, darle marca, y **darle un administrador**. Un tenant sin admin es una carpeta vacía —no hay quién entre a crear al resto— y por eso el alta del usuario vive acá y no en `Configuración › Usuarios`, que asigna roles a gente que ya existe. La fila del tenant activo lo dice con todas sus letras cuando falta.
    - **El correo es la CREDENCIAL y el código es la IDENTIDAD.** El correo no se repite (dos personas con el mismo correo son la misma sesión) y lo resuelve `codigoDeCorreo`, que consulta la lista **viva**: el admin recién creado entra **sin recargar**. El código se deriva del nombre y queda **fijo** —lo guardan `deal.exec`, la auditoría y los hilos, así que renombrar a alguien no puede convertirlo en otro— y nunca se reusa uno vivo, que sería atribuirle a alguien lo que hizo otro.
    - **La marca se mudó desde `Funcionalidades`**, donde estaba junto a los toggles de módulos. Logotipo, nombre y acento son **identidad del tenant**, no una funcionalidad suya; `Funcionalidades` decide qué módulos ve el ejecutivo.
    - **EL SUPER-ADMIN ES UN ROL, NO UN CÓDIGO.** `atribDeRol` decía «la atribución sigue al ROL» y resolvía el super-admin por el código literal `"ADMIN"`, que es el del elenco de la demo. Medido al dar de alta el primer administrador: salía con atribución **vacía** y **no entraba al padrón**, o sea que el administrador del tenant nuevo no podía aprobar nada —que es justo para lo que se lo crea—. Lo mismo pasaba con los siete permisos de visibilidad y con `esJefeComercial`/`esGerenteComercial`. Ahora todos preguntan por `esRolAdmin`. `ROL_ATRIB` no declara `admin` a propósito: cubre las tres áreas en el nivel máximo y no un par (área, nivel).
    - **Toda pantalla de Configuración dice sobre qué tenant está configurando** (pedido del usuario). Va en el CONTENEDOR y no en cada sección: son veinte pantallas y la que se agregue mañana lo tendría que recordar sola. `Tenants` es la única que no configura un tenant sino la lista de todos, y lo dice en vez de mentir —un rótulo que afirma lo mismo en todas partes deja de significar algo—.
    - **`Configuración › Áreas` perdió las columnas «Criterios que rutean acá» y «Quién la tiene»** (pedido del usuario: «elimina toda esa información»). Lo que **no** se fue es el control de la **regla 35**: el aviso de los criterios MAL DEFINIDOS, que es lo que su gate exige y lo que hace visible que borrar un área deja criterios huérfanos. Tampoco se fue la guarda de borrado: `tramosDeArea` ya no se muestra, pero sigue decidiendo si el área se puede eliminar.

70. **LA EXCEPCIÓN QUE LA VERSIÓN N YA NO LEVANTA SE MARCA «YA NO APLICA DESDE LA VERSIÓN N», NO SE BORRA; Y LA MARCADA NO
    SE REACTIVA** (23-09-2026, ADR-0016, M-21, G-14 · G-35; el usuario, 22-09-2026: «no debería quedar huérfano, debería
      quedar con un estado que identifique que cambió, para poder auditar que esa regla quedó así en el cambio de
      versión»). Antes, cuando una re-evaluación dejaba de levantar una excepción ya solicitada o visada, el criterio pasaba
      a cumplido y el visado, la solicitud, la tarea y el hilo quedaban HUÉRFANOS: una aprobación sobre una regla que ya no
      gatilla, indistinguible de una vigente. El modelo decía «se elimina»; borrar pierde la traza de quién aprobó y de por
      qué dejó de hacer falta.
    - **La decisión es pura** (`excepcionesQueYaNoAplican(items, sol, st, det, version, fecha)`): compara las claves con
      solicitud o visado contra lo que la evaluación vigente levanta como excepción; lo que ya no levanta —porque la variable
      se regularizó o porque el deudor salió de la operación— recibe la marca `{desdeVersion: N, por: "sistema", fecha}`: la
      solicitud pasa a `estado: "no_aplica"` y el visado a `"no_aplica"` conservando en su detalle la decisión del apoderado
      (`decision`, quién, cuándo). Lo que sigue gatillando no se toca; lo ya marcado no se marca dos veces. La función sólo
      escribe la marca —nunca «aprobado» ni «rechazado»: el sistema no decide—, y por eso el gate de OTG-01 exime ese camino
      con esa condición comprobada.
    - **La mutación** (`marcarExcepcionesQueYaNoAplican`, llamada por `evaluarOperacion` —el evento de evaluación, regla 72— tras emitir la versión y con su
      número) escribe los tres repositorios, cierra la tarea del aprobador con el motivo (la tarea conoce su `stKey` desde
      que se crea), postea en el hilo «Aprobación de excepciones» como el SISTEMA (`CODE_SISTEMA`) y lo termina sólo cuando ya
      no queda ninguna excepción por visar en la operación —el hilo es por operación, no por excepción—, y deja fila en la
      auditoría y en la bitácora de otorgamiento con actor `-- Sistema --`. Nada se borra.
    - **La marca no es una decisión.** `excSinVisar(st, k)` es la única lectura que decide si una excepción está pendiente
      (sin visado o con la marca) y `solVigente(sol, k)` la única que dice si tiene solicitud vigente: los doce lectores del
      visado —`visadoDealCalc`, `faseOtorgDeal`, `excepcionesSinComentario`, los avisos, la mesa, los contadores— pasan por
      ahí, y el gate prohíbe el `!st[x.stKey]` suelto que volvería a tratar la marca como aprobación. Si una versión
      posterior vuelve a levantar la excepción, está pendiente OTRA VEZ: la solicitud marcada no justifica la de hoy (regla
      62) ni se reactiva; el ejecutivo solicita de nuevo y la solicitud nueva anota su `version` y lleva la anterior en
      `anteriores`; el apoderado visa de nuevo y el detalle nuevo hereda la historia del marcado (`historiaVisado`).
    - **Se ve.** En el tab Otorgamiento el criterio cumplido muestra «↺ Excepción anterior · ya no aplica desde la versión N ·
      sistema · hora» con la solicitud y el visado que tuvo, **en un balde propio y siempre a la vista** —dentro del
      colapsable de «N regla(s) aprobada(s)» la marca no la veía nadie, que es lo que se midió al abrir la pantalla—; la
      excepción cuyo deudor salió de la operación —sin fila donde mostrarse— va en su propia lista; en la bandeja de Tareas la tarea dice «Cerrada por el sistema» y por qué; la mesa de
      Otorgamientos y el tab la muestran pendiente otra vez, con la anterior debajo, cuando vuelve a levantar. Reabrir a
      mano la tarea borra el cierre del sistema.
    - **Lo que enseñó medirlo:** `evaluarOtorgItems` pisa las variables versionadas con `varsModeloExt(deal)` (pagaré,
      cliente nuevo, mora interna…), así que de las que `snapVersionCli` regulariza sólo cambian de disposición las que el
      modelo no recalcula en vivo —C07 «Cupo suficiente» (`mntLinea`), concentraciones, notas de crédito—; O05 sigue en
      excepción porque la evidencia del contrato es de la operación, no de la versión. Por eso la fixture del caso es C07.
    - Caso **166** (la solicitud marcada con versión, actor y hora; tarea cerrada con motivo, mensaje del sistema, auditoría
      y bitácora; la que sigue gatillando intacta; el visado marcado conserva la decisión; el deudor que sale; la que vuelve
      a levantar está pendiente, la marcada no justifica ni se reactiva, la solicitud nueva con historia; la decisión pura
      con entradas plantadas) y `regla_70.test.mjs` (la llamada tras la versión, la decisión que sólo marca, la mutación que
      no borra, ningún lector suelto, la historia, la pantalla y el balde propio: trece sondas). La pantalla completa sigue por e2e (CP-124).
