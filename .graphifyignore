# Exclusiones para graphify. Sintaxis de .gitignore; el .gitignore del repo ya se respeta solo.
# Acá va lo TRACKEADO que no es código de este proyecto o que es generado.

# Generado por GeneradorDatos/ (~34 MB). El origen sí entra al grafo; el producto no.
datos_inyectados.js

# Bytes fijados por el SBOM: React y Tailwind vendorizados. No son código nuestro.
vendor/

# Código retirado, conservado sólo como historia.
Legado/

# DOM capturado por capturar_pantallas.mjs: es salida, no fuente.
Capturas_UI/
Variantes_UI/

# Skills de terceros instaladas en el repo (Leonxlnx/taste-skill) y referencias de diseño.
.claude/skills/
Skills/design-md/

# Servidores MCP de terceros clonados y construidos.
.mcp-servers/*/upstream/
